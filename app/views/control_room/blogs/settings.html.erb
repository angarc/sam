<h1>Settings for <%= @item.name.titleize %></h1>

<h3 class='mt-5'>Excel Upload</h3>
<div class='upload card'>
  <div class='card-body'>
    <p>You can upload an Excel sheet (.xlsx) to pre fill out all your blog info when you create a new blog using S.A.M.<br>
    Just make sure it has the same structure has the base excel file. If you added any new columns they will be ignored.<br>
    But if you completely changed the structure of the excel sheets, it's not going to import correctly.</p>
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#blogSpreadsheetImport">
      <%= fa_icon('upload', text: 'Upload') %>
    </button>
  </div>
</div>

<!-- Modal -->
<div class="modal fade" id="blogSpreadsheetImport" tabindex="-1" role="dialog" aria-labelledby="blogSpreadsheetImportTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="blogSpreadsheetImportTitle">Upload an Excel Sheet</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= simple_form_for @spreadsheet, url: process_spreadsheet_control_room_blog_path(@item) do |f| %>
          <%= f.association :blog, collection: current_user.blogs, selected: @blog %>

          <%= f.input :spreadsheet, input_html: { class: 'form-control-file' } %>

          <%= f.submit 'Upload', class: 'btn btn-md btn-primary'%>
        <% end %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>